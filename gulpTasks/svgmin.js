const gulp = require('gulp'),  l = require('gulp-load-plugins')(),  combine = require('stream-combiner2').obj;module.exports = function(options){  return function () {    return combine(      gulp.src('src/blocks/icons-svg/svg_sprite.twig'),      l.svgmin({        js2svg: {          pretty: true        }      }),      gulp.dest('src/blocks/icons-svg/'),     l.debug({title: 'min svg files'}).on('error', l.notify.onError())    )  }};